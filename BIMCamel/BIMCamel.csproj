<Project Sdk="Microsoft.NET.Sdk.WindowsDesktop">

  <PropertyGroup>
    <TargetFramework>net48</TargetFramework>
    <UseWindowsForms>true</UseWindowsForms>
    <Nullable>enable</Nullable>
    <LangVersion>10.0</LangVersion>
    <AssemblyName>BIMCamel</AssemblyName>
    <RootNamespace>BIMCamel</RootNamespace>
    <PlatformTarget>x64</PlatformTarget>
    <OutputType>Library</OutputType>
    <Product>BIMCamel IFC Exporter</Product>

    <!--
      NavisworksDir is the install used for COMPILE-TIME references only (Private=False,
      so the DLLs are never copied). IMPORTANT: the resulting DLL is bound to that version's
      Autodesk.Navisworks.Api (2024 = 21.x, 2025 = 22.x, 2026 = 23.x) and is REJECTED by other
      Navisworks versions at load (PLUGIN_LOAD_07). So build once per version you want to support:
        dotnet build -p:NavisworksDir="C:\...\Navisworks Manage 2025"
      NavisYear is the matching year folder for that build (auto-derived from the last 4 chars of
      NavisworksDir; override with -p:NavisYear=2025 if your path doesn't end in the year).
    -->
    <NavisworksDir Condition="'$(NavisworksDir)' == ''">C:\Program Files\Autodesk\Navisworks Manage 2024</NavisworksDir>
    <NavisYear Condition="'$(NavisYear)' == ''">$(NavisworksDir.Substring($([MSBuild]::Subtract($(NavisworksDir.Length), 4))))</NavisYear>
    <BundleRoot>$(APPDATA)\Autodesk\ApplicationPlugins\BIMCamel.bundle</BundleRoot>
  </PropertyGroup>

  <!-- ── Navisworks API references (compile-time only) ─────────────────────── -->
  <ItemGroup>
    <Reference Include="Autodesk.Navisworks.Api">
      <HintPath>$(NavisworksDir)\Autodesk.Navisworks.Api.dll</HintPath>
      <Private>False</Private>
    </Reference>
    <Reference Include="Autodesk.Navisworks.ComApi">
      <HintPath>$(NavisworksDir)\Autodesk.Navisworks.ComApi.dll</HintPath>
      <Private>False</Private>
    </Reference>
    <Reference Include="Autodesk.Navisworks.Interop.ComApi">
      <HintPath>$(NavisworksDir)\Autodesk.Navisworks.Interop.ComApi.dll</HintPath>
      <Private>False</Private>
    </Reference>
    <!-- System.Numerics provides BigInteger, used by IfcGuid -->
    <Reference Include="System.Numerics" />
    <!-- DataContractJsonSerializer for export profiles (F10) -->
    <Reference Include="System.Runtime.Serialization" />
  </ItemGroup>

  <!-- ── XAML ribbon layout is a Navisworks content file, NOT WPF-compiled ─── -->
  <ItemGroup>
    <None Remove="BIMCamel.xaml" />
    <Content Include="BIMCamel.xaml">
      <CopyToOutputDirectory>Never</CopyToOutputDirectory>
    </Content>
  </ItemGroup>

  <!-- ── Dev convenience: deploy to the per-user .bundle on Debug builds only.
       Deploys ONLY to the $(NavisYear) folder matching the API this build was compiled against —
       copying one DLL into every year folder would produce bundles that crash on load in the
       non-matching versions. Release builds are for the installer, so they skip this. ── -->
  <Target Name="DeployToBundle" AfterTargets="Build" Condition="'$(Configuration)' == 'Debug'">
    <ItemGroup>
      <DepDlls Include="@(ReferenceCopyLocalPaths)" Condition="'%(Extension)' == '.dll'" />
      <IconFiles Include="$(ProjectDir)Resources\*.png" />
    </ItemGroup>

    <MakeDir Directories="$(BundleRoot)\$(NavisYear)\en-US;$(BundleRoot)\$(NavisYear)\Resources" />

    <Copy SourceFiles="$(OutputPath)$(AssemblyName).dll"
          DestinationFolder="$(BundleRoot)\$(NavisYear)" SkipUnchangedFiles="true" />
    <Copy SourceFiles="$(OutputPath)$(AssemblyName).pdb"
          DestinationFolder="$(BundleRoot)\$(NavisYear)" SkipUnchangedFiles="true"
          ContinueOnError="true" />
    <Copy SourceFiles="@(DepDlls)"
          DestinationFolder="$(BundleRoot)\$(NavisYear)" SkipUnchangedFiles="true"
          ContinueOnError="true" />
    <Copy SourceFiles="$(ProjectDir)BIMCamel.xaml"
          DestinationFolder="$(BundleRoot)\$(NavisYear)\en-US" SkipUnchangedFiles="true" />
    <Copy SourceFiles="@(IconFiles)"
          DestinationFolder="$(BundleRoot)\$(NavisYear)\Resources" SkipUnchangedFiles="true"
          ContinueOnError="true" />

    <Copy SourceFiles="$(ProjectDir)..\PackageContents.xml"
          DestinationFolder="$(BundleRoot)" SkipUnchangedFiles="true" />

    <Message Text="BIMCamel ($(NavisYear), built against $(NavisworksDir)) deployed to $(BundleRoot)\$(NavisYear)" Importance="High" />
  </Target>

</Project>
